import React from "react";
import { OnboardPageProps } from "../../models/OnboardPageProps";

const OnboardReview = (props: OnboardPageProps) => {
  return (
    <div>
      <h1>Onboard Review</h1>
    </div>
  );
};

export default OnboardReview;
